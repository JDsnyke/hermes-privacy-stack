# Interactive site

`site/` is a dependency-free, tracking-free static configurator. It uses no external fonts, analytics, CDN scripts or network calls. It can be hosted on GitHub Pages or opened directly from disk.

The configurator only generates commands/config in browser memory. It does not collect credentials and should never ask for secrets.
