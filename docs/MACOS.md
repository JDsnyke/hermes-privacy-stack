# macOS

Install Git, Python 3 and Docker Desktop, then run the one-line installer from README. Hermes' official CLI installer supports macOS; Hermes Desktop is also available upstream.

Apple Silicon is the preferred local-container path. Intel Macs may have different Hindsight package/image resource characteristics; use Docker and validate with `scripts/doctor.py`.
