# Linux

Install Git, Python 3.10+, Docker Engine and Compose v2. Add your user to the Docker group only if you understand that Docker group membership is effectively root-equivalent authority.

Run the one-line installer from README. On a server, keep container ports loopback-bound and use Tailscale/WireGuard for remote clients rather than changing them to `0.0.0.0`.
