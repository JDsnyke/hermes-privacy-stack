# Document Intake

For local documents: classify sensitivity first. Prefer Docling for PDFs, Office files, tables and layout-heavy content. Prefer lightweight text conversion only when structure is simple. Do not upload private documents to external converters when the local stack can parse them. Store reusable extracted knowledge in the knowledge layer; store personal/project conclusions in memory only when durable.
