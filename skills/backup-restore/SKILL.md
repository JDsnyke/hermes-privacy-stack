# Backup / Restore

Backups must be encrypted before leaving the device. Prefer logical Hindsight bank export over copying live database files. Verify backups by restoring into a temporary destination and running recall checks. Back up local configuration separately from credentials; if credential backup is desired, use an encrypted secret manager/archive and document the recovery key procedure.
